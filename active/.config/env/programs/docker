# NOTE: this is not linked via the init script as it doubles terminal init time. Just source this file from any tmux project that needs it.

# boot2docker init.
# eval $(boot2docker shellinit 2>/dev/null)

# export DOCKER_HOST=tcp://192.168.59.103:2376
# export DOCKER_CERT_PATH=/Users/wilmoore/.boot2docker/certs/boot2docker-vm
# export DOCKER_TLS_VERIFY=0

# docker toolbox
docker-machine start default >/dev/null
eval "$(docker-machine env default)"
